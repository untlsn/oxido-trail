{
  "name": "openai",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "start": "bun run ./index.ts",
		"start:full": "bun run ./index.ts --with-template",
		"compile": "bun build ./index.ts --outfile ./index.js --target node --external '*'"
  },
  "devDependencies": {
    "@types/bun": "latest",
		"typescript": "^5.0.0"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "openai": "^4.72.0"
  }
}